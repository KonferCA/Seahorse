{
    "version": 2, // Deployment configuration version
    "framework": "nextjs",
    "buildCommand": "pnpm build", // Command to build the Next.js project
    "installCommand": "pnpm i",
    "outputDirectory": ".next", // Directory for the build output
    "regions": ["sfo1"],
    "env": {
      "NEXT_PUBLIC_NETWORK_ID": "testnet"
    }
}
